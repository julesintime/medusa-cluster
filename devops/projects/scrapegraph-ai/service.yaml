apiVersion: v1
kind: Service
metadata:
  name: scrapegraph-ai
  namespace: scrapegraph-ai
  labels:
    app: scrapegraph-ai
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: scrapegraph-ai