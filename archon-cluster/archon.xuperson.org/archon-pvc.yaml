apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archon-workspace-pvc
  namespace: archon
  labels:
    app.kubernetes.io/name: archon
    app.kubernetes.io/component: workspace
    app.kubernetes.io/part-of: archon-cluster
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: longhorn
