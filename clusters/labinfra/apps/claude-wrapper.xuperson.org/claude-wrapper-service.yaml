apiVersion: v1
kind: Service
metadata:
  name: claude-wrapper
  namespace: claude-wrapper
  labels:
    app: claude-wrapper
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: claude-wrapper