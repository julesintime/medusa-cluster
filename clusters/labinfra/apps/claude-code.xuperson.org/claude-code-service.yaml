apiVersion: v1
kind: Service
metadata:
  name: claude-code-wrapper
  namespace: claude-code
  labels:
    app: claude-code-wrapper
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: claude-code-wrapper