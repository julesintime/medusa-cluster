apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: medusa-registry-credentials
  namespace: medusa
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60
  
  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator
  
  managedKubeSecretReferences:
    - secretName: medusa-registry-secret
      secretNamespace: medusa
      creationPolicy: "Orphan"
      secretType: kubernetes.io/dockerconfigjson
      template:
        data:
          .dockerconfigjson: |
            {
              "auths": {
                "git.xuperson.org": {
                  "username": "{{ .GITEA_ADMIN_USERNAME.Value }}",
                  "password": "{{ .GITEA_ADMIN_PASSWORD.Value }}",
                  "auth": "{{ printf "%s:%s" .GITEA_ADMIN_USERNAME.Value .GITEA_ADMIN_PASSWORD.Value | b64enc }}"
                }
              }
            }