---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: medusa-postgres-secret
  namespace: medusa
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60

  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator

  managedKubeSecretReferences:
    - secretName: medusa-postgres-secret
      secretNamespace: medusa
      creationPolicy: "Orphan"
      template:
        data:
          postgres-password: "{{ .MEDUSA_POSTGRES_ADMIN_PASSWORD.Value }}"
          password: "{{ .MEDUSA_POSTGRES_PASSWORD.Value }}"

---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: medusa-redis-secret
  namespace: medusa
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60

  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator

  managedKubeSecretReferences:
    - secretName: medusa-redis-secret
      secretNamespace: medusa
      creationPolicy: "Orphan"
      template:
        data:
          password: "{{ .MEDUSA_REDIS_PASSWORD.Value }}"

---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: medusa-app-secrets
  namespace: medusa
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60

  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator

  managedKubeSecretReferences:
    - secretName: medusa-app-secrets
      secretNamespace: medusa
      creationPolicy: "Orphan"
      template:
        data:
          DATABASE_URL: "{{ .MEDUSA_DATABASE_URL.Value }}"
          REDIS_URL: "{{ .MEDUSA_REDIS_URL.Value }}"
          JWT_SECRET: "{{ .MEDUSA_JWT_SECRET.Value }}"
          COOKIE_SECRET: "{{ .MEDUSA_COOKIE_SECRET.Value }}"
          ADMIN_EMAIL: "{{ .MEDUSA_ADMIN_EMAIL.Value }}"
          ADMIN_PASSWORD: "{{ .MEDUSA_ADMIN_PASSWORD.Value }}"

---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: registry-credentials
  namespace: medusa
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60

  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator

  managedKubeSecretReferences:
    - secretName: registry-credentials
      secretNamespace: medusa
      creationPolicy: "Orphan"
      template:
        data:
          .dockerconfigjson: "{{ .REGISTRY_DOCKER_CONFIG_JSON.Value }}"