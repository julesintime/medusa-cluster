apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Core infrastructure
  - gitlab-namespace.yaml
  - gitlab-rbac.yaml

  # Networking
  - gitlab-ingress.yaml

  # GitLab application
  - gitlab-helmrepository.yaml
  - gitlab-helmrelease.yaml

  # GitLab Runner
  - gitlab-runner-helmrelease.yaml

  # Secrets management (Infisical)
  - gitlab-infisical-secrets.yaml