---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: buildkit-daemon-certs
  namespace: gitea
spec:
  authentication:
    serviceToken:
      secretsScope:
        envSlug: dev
        recursive: false
        secretsPath: /
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator
  hostAPI: https://app.infisical.com/api
  managedKubeSecretReferences:
  - creationPolicy: Orphan
    secretName: buildkit-daemon-certs
    secretNamespace: gitea
    secretType: Opaque
    template:
      data:
        ca.pem: '{{ .BUILDKIT_CA_PEM.Value }}'
        cert.pem: '{{ .BUILDKIT_CERT_PEM.Value }}'
        key.pem: '{{ .BUILDKIT_KEY_PEM.Value }}'
        client-cert.pem: '{{ .BUILDKIT_CLIENT_CERT_PEM.Value }}'
        client-key.pem: '{{ .BUILDKIT_CLIENT_KEY_PEM.Value }}'
  resyncInterval: 60
