apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: hello-setup

resources:
  - hello-app-configmap.yaml
  - hello-cicd-configmap.yaml
  - hello-repo-init-job.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/name: hello-setup
      app.kubernetes.io/managed-by: flux