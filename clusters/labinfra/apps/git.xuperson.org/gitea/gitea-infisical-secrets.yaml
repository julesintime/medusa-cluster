---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: gitea-secrets
  namespace: gitea
spec:
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60
  
  authentication:
    serviceToken:
      secretsScope:
        envSlug: prod
        secretsPath: "/"
        recursive: false
      serviceTokenSecretReference:
        secretName: infisical-service-token
        secretNamespace: infisical-operator
  
  managedKubeSecretReferences:
    - secretName: gitea-admin-secrets
      secretNamespace: gitea
      creationPolicy: "Orphan"
      template:
        data:
          username: "{{ .GITEA_ADMIN_USERNAME.Value }}"
          password: "{{ .GITEA_ADMIN_PASSWORD.Value }}"
          email: "{{ .GITEA_ADMIN_EMAIL.Value }}"
        # Note: Admin token is now created dynamically by hello-labinfra-repo-init job
        # This eliminates the need to store long-lived tokens in Infisical
