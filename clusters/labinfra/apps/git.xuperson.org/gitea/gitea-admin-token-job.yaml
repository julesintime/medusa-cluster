---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: gitea-admin-token-sync
  namespace: gitea
  labels:
    app.kubernetes.io/name: gitea-admin-token-sync
    app.kubernetes.io/component: token-management
    app.kubernetes.io/managed-by: flux
spec:
  schedule: "*/5 * * * *"  # Run every 5 minutes
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 300
      template:
        metadata:
          labels:
            app.kubernetes.io/name: gitea-admin-token-sync
        spec:
          restartPolicy: OnFailure
          serviceAccountName: gitea-runner-init
          initContainers:
          - name: wait-for-gitea
            image: curlimages/curl:latest
            command:
            - /bin/sh
            - -c
            - |
              echo "🔄 Waiting for Gitea to be ready..."
              until curl -f -s http://gitea-http:3000/api/v1/version; do
                echo "   Gitea not ready, waiting 10 seconds..."
                sleep 10
              done
              echo "✅ Gitea is ready!"
          containers:
          - name: sync-admin-token
            image: curlimages/curl:latest
            env:
            - name: GITEA_ADMIN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: gitea-admin-secrets
                  key: username
            - name: GITEA_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gitea-admin-secrets
                  key: password
            command:
            - /bin/sh
            - -c
            - |
              set -e
              echo "🔄 Syncing Gitea admin token..."

              # List existing tokens
              echo "📋 Listing existing admin tokens..."
              TOKENS=$(curl -s "http://gitea-http:3000/api/v1/users/$GITEA_ADMIN_USERNAME/tokens" \
                -u "$GITEA_ADMIN_USERNAME:$GITEA_ADMIN_PASSWORD")

              echo "Tokens response: $TOKENS"

              # Check if we have any tokens (simple string check since jq may not be available)
              if echo "$TOKENS" | grep -q '"id":'; then
                echo "✅ Found existing tokens, cleaning up..."
                # Extract token IDs using grep and sed
                TOKEN_IDS=$(echo "$TOKENS" | grep -o '"id":[0-9]*' | sed 's/"id"://g')
                for token_id in $TOKEN_IDS; do
                  echo "Deleting token ID: $token_id"
                  curl -s -X DELETE "http://gitea-http:3000/api/v1/users/$GITEA_ADMIN_USERNAME/tokens/$token_id" \
                    -u "$GITEA_ADMIN_USERNAME:$GITEA_ADMIN_PASSWORD" || echo "Delete failed for $token_id"
                done
              else
                echo "ℹ️ No existing tokens found"
              fi

              # Always create the single admin token
              echo "🏗️ Creating admin token..."
              RESPONSE=$(curl -s -X POST "http://gitea-http:3000/api/v1/users/$GITEA_ADMIN_USERNAME/tokens" \
                -H "Content-Type: application/json" \
                -u "$GITEA_ADMIN_USERNAME:$GITEA_ADMIN_PASSWORD" \
                -d '{
                  "name": "flux-automation",
                  "scopes": ["read:activitypub", "read:issue", "write:misc", "read:notification", "read:organization", "read:package", "read:repository", "read:user", "write:admin", "write:repository", "write:user"]
                }')

              echo "Create token response: $RESPONSE"
              # Extract token using grep/sed instead of jq
              ADMIN_TOKEN=$(echo "$RESPONSE" | grep -o '"sha1":"[^"]*"' | sed 's/"sha1":"//;s/"//')
              TOKEN_NAME="flux-automation"

              if [ -z "$ADMIN_TOKEN" ] || [ "$ADMIN_TOKEN" = "null" ]; then
                echo "❌ Failed to get admin token"
                exit 1
              fi

              echo "✅ Admin token ready: $TOKEN_NAME (length: ${#ADMIN_TOKEN})"

              # Update the Kubernetes secret
              cat <<EOF | kubectl apply -f -
              apiVersion: v1
              kind: Secret
              metadata:
                name: gitea-admin-api-token
                namespace: gitea
              type: Opaque
              data:
                token: $(echo -n "$ADMIN_TOKEN" | base64)
              EOF

              echo "💾 Admin token synced to secret 'gitea-admin-api-token'"

              # Update the Kubernetes secret
              cat <<EOF | kubectl apply -f -
              apiVersion: v1
              kind: Secret
              metadata:
                name: gitea-admin-api-token
                namespace: gitea
              type: Opaque
              data:
                token: $(echo -n "$ADMIN_TOKEN" | base64)
              EOF

              echo "💾 Admin token synced to secret 'gitea-admin-api-token'"

            resources:
              requests:
                memory: "32Mi"
                cpu: "50m"
              limits:
                memory: "64Mi"
                cpu: "100m"
