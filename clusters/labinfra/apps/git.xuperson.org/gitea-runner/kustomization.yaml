apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: gitea-runner

resources:
  - gitea-runner-rbac.yaml
  - gitea-runner-token-job.yaml
  - gitea-runner-dind.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/name: gitea
      app.kubernetes.io/managed-by: flux
