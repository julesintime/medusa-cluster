apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Core infrastructure
  - airflow-namespace.yaml
  - airflow-rbac.yaml

  # Networking
  - airflow-ingress.yaml

  # Apache Airflow application
  - airflow-helmrepository.yaml
  - airflow-helmrelease.yaml

  # Secrets management (Infisical)
  - airflow-infisical-secrets.yaml