apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-cicd-demo
  namespace: hello
  labels:
    app.kubernetes.io/name: hello-cicd-demo
    app.kubernetes.io/part-of: hello
    app.kubernetes.io/component: web
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hello-cicd-demo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hello-cicd-demo
        app.kubernetes.io/part-of: hello
        app.kubernetes.io/component: web
    spec:
      containers:
      - name: hello-cicd-demo
        image: git.xuperson.org/demo/hello-cicd-demo:latest # {"$imagepolicy": "flux-system:hello-cicd-demo"}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5