apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-code-api
  namespace: claude-code-api
  labels:
    app: claude-code-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claude-code-api
  template:
    metadata:
      labels:
        app: claude-code-api
    spec:
      imagePullSecrets:
      - name: gitea-registry-secret
      containers:
      - name: claude-code-api
        image: git.xuperson.org/helloroot/claude-code-openai-wrapper:main-90d2f39 # {"$imagepolicy": "claude-code-api:claude-code-api"}
        ports:
        - containerPort: 8000
        env:
        - name: PORT
          value: "8000"
        - name: PYTHONPATH
          value: "/app"
        envFrom:
        - secretRef:
            name: claude-code-api-secrets
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10