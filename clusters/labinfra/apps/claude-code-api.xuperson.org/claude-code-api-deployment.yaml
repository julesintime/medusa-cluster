apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-code-api
  namespace: claude-code-api
  labels:
    app: claude-code-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claude-code-api
  template:
    metadata:
      labels:
        app: claude-code-api
    spec:
      imagePullSecrets:
      - name: gitea-registry-secret
      containers:
      - name: claude-code-api
        image: git.xuperson.org/helloroot/claude-code-openai-wrapper:main-c3a0def # {"$imagepolicy": "claude-code-api:claude-code-api"}
        ports:
        - containerPort: 8000
        env:
        - name: PORT
          value: "8000"
        - name: PYTHONPATH
          value: "/app"
        envFrom:
        - secretRef:
            name: claude-code-api-secrets
        volumeMounts:
        - name: claude-auth
          mountPath: /root/.config/claude-code
          readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: claude-auth
        secret:
          secretName: claude-code-api-secrets
          items:
          - key: auth.json
            path: auth.json
