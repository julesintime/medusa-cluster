apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespaces.yaml
  - external-dns.yaml
  - cloudflared.yaml
  - helmrepository.yaml
  # TODO: Re-enable after configuring Infisical secrets properly
  # - cloudflare-sealed-secrets.yaml