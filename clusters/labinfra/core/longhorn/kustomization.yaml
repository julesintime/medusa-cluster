apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - longhorn-namespace.yaml
  - longhorn-helmrepository.yaml
  - longhorn.yaml
  - storageclass-fast.yaml
  - storageclass-retention.yaml  # StorageClasses with Retain policy for critical data

# Resources define their own namespaces